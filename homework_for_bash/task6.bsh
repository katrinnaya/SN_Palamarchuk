#!/bin/bash

# 1. Читаем данные из файла input.txt и выводим их на экран
cat input.txt

# 2. Подсчитываем количество строк в файле input.txt с помощью команды wc -l
# и перенаправляем результат в файл output.txt
wc -l < input.txt > output.txt

# 3. Пытаемся выполнить команду ls для несуществующего файла,
# перенаправляя возможные ошибки в файл error.log
ls nonexistent_file 2> error.log

#Оператор < для передачи содержимого файла на вход команде wc, а оператор > для записи результата в файл.
